import React from 'react';

const ChatBar = () => {
    return (
        <div className="chat__bar">
            <h2>Active Users</h2>
            {/* List of active users */}
        </div>
    );
};

export default ChatBar;